# RTM Protocol
These are scraped from the webpack chunks of EA Desktop's frontend using Qt's remote debugger.

A useful way of testing these protos and figuring out new ones is passing a captured message into protoc's `--decode_raw` mode.

EADP stands for EA Digital Platform. Antelope is the name of the RTM protocol.
